type AuthPayload {
  token: String!
  user: User!
}

# Tipos base — todos os outros arquivos usam extend type
type Query
type Mutation
type Subscription

extend type Mutation {
  login(email: String!, password: String!): AuthPayload!
  register(data: CreateUserInput!): AuthPayload!
}